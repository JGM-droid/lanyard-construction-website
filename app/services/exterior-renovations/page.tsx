import { PageContainer } from '@/components/page-container';
import { SectionWrapper } from '@/components/section-wrapper';
import { PlaceholderContent } from '@/components/placeholder-content';

export default function ExteriorRenovationsPage() {
  return (
    <PageContainer>
      <SectionWrapper>
        <PlaceholderContent
          title="Exterior Renovations"
          eyebrow="Placeholder foundation"
          notice="This service detail route shell is intentionally minimal and will be expanded once the services feature agent begins implementation."
        />
      </SectionWrapper>
    </PageContainer>
  );
}
